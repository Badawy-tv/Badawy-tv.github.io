document.getElementById("askButton").addEventListener("click", async () => { let question = prompt("What would you like to ask?"); if (question) { const res = await fetch("/query", { method: "POST", headers: {"Content-Type": "application/x-www-form-urlencoded"}, body: new URLSearchParams({question}) }); const data = await res.json(); alert("AI Answer: " + data.answer); } });
